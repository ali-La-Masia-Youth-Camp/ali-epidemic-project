import { Component, Vue } from 'vue-property-decorator';

@Component({})
export default class CountryMap extends Vue {
    public render() {
        return (
            <div>地图</div>
        );
    }
}
